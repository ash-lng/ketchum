/*

import Basics exposing (never)
import Elm.Kernel.Scheduler exposing (binding)
import Task exposing (perform)

*/



// ELEMENT


var _AshCoreModCool_alert = function(message)
{
  alert("msg " + message);
};
